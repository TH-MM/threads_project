const Logo = ({className}) => {
    return(
        <h1 className={`text-2xl font-bold ${className} text-red-400`}>M<span className={`text-2xl font-bold ${className} text-gray-900`}>BLOG</span></h1>
    )
}

export default Logo;